# TunnelR (PicoTun) 🚀

**نسل جدید تانل معکوس برای عبور از فیلترینگ پیشرفته با حداکثر پایداری و سرعت** — `v2.8.1`

این پروژه یک بازنویسی کامل و بهینه‌سازی شده از PicoTun است که با **Web Dashboard**، معماری **Dagger**، و قابلیت‌های پیشرفته‌ای مثل **Warm Standby**، **Adaptive FrameSize**، و **Latency Routing** تجربه‌ای بدون قطعی و با سرعت بالا را در شرایط سخت شبکه فراهم می‌کند.

### 📊 دسترسی به داشبورد
بعد از نصب، داشبورد به صورت پیش‌فرض فعال است:
1. پورت فایروال را باز کنید: `ufw allow 8080/tcp`
2. مرورگر را باز کنید: `http://SERVER_IP:8080`
3. اگر بالا نیامد، فایل کانفیگ را چک کنید که `dashboard.listen` روی `0.0.0.0:8080` باشد.

---

## چرا TunnelR؟ ✨

### 🛡️ پایداری بی‌نظیر (Rock-Solid Stability)
- **Smart Health Check (v2.5.0)**: سیستم هوشمند تشخیص و حذف کانکشن‌های مرده (Zombie Sessions) در کمتر از ۱۰ ثانیه. اگر اینترنت قطع و وصل شود، تانل خودکار و فوری ترمیم می‌شود.
- **Connection Recycling (v2.5.1)**: بازسازی خودکار کانکشن‌ها هر ۲۰ دقیقه برای جلوگیری از Throttling توسط ISP/DPI. بدون قطعی دانلود! 🔄
- **Connection Pooling**: برقراری چندین کانکشن موازی (Multi-Session Smux) برای افزایش Throughput و جلوگیری از افت سرعت در تک‌کانکشن‌ها.

### 🚀 سرعت و پرفورمنس (High Performance)
- **DPI Bypass (v2.4.6)**: بهینه‌سازی سایز فریم‌ها (2KB) برای عبور از محدودیت‌های سرعت و Throttling روی پورت‌های خاص.
- **Memory Optimization (v2.5.0)**: استفاده از `sync.Pool` برای مدیریت حافظه و کاهش فشار روی CPU در ترافیک‌های سنگین.
- **Snappy Compression**: فشرده‌سازی هوشمند ترافیک برای کاهش مصرف پهنای باند و افزایش سرعت لود صفحات.

### 🔒 امنیت و اختفا (Security & Stealth)
- **HTTP Mimicry**: ترافیک کاملاً شبیه به وب‌گردی عادی (HTTPS به google.com) برای فریب فایروال.
- **TLS Fragmentation**: شکستن بسته ClientHello برای دور زدن سیستم‌های تشخیص DPI.
- **uTLS Fingerprint**: شبیه‌سازی دقیق فینگرپرینت مرورگر Chrome 120.
- **AES-256-GCM**: رمزنگاری نظامی تمام بسته‌ها برای امنیت کامل دیتا.

---

## 📦 نصب آسان (Linux)

تنها با یک دستور، اسکریپت هوشمند نصب و پیکربندی را انجام می‌دهد:

```bash
bash <(curl -s https://raw.githubusercontent.com/ramin-mahmoodi/tunnelR/main/setup.sh)
```

**امکانات منوی نصب:**
1.  **نصب سرور (ایران)**: تنظیم خودکار، ساخت سرویس systemd، و بهینه‌سازی کرنل.
2.  **نصب کلاینت (خارج)**: تنظیم خودکار، انتخاب تعداد Connection Pool (پیش‌فرض ۸) و تنظیمات Transport.
3.  **مدیریت**: مشاهده لاگ‌ها، ریستارت سرویس، و آپدیت خودکار به آخرین نسخه.

---

## ⚙️ پیکربندی پیشرفته

### کلاینت (Client)
فایل: `/etc/picotun/client.yaml`

```yaml
mode: "client"
psk: "YOUR_SECRET_PSK"
transport: "httpsmux"    # httpsmux (توصیه شده), tcpmux
profile: "stable"        # بهترین پروفایل برای پایداری

paths:
  - addr: "IRAN_SERVER_IP:2020"
    transport: "httpsmux"
    connection_pool: 8   # تعداد کانکشن‌های همزمان (پیشنهاد: 8 یا 16 برای سرعت بالا)
    dial_timeout: 10

# جلوگیری از نشت DNS
dns:
  enabled: true
  listen: "127.0.0.1:53"
  upstream: "8.8.8.8:53"

# فشرده‌سازی (اختیاری)
compression: "snappy"
```

### سرور (Server)
فایل: `/etc/picotun/server.yaml`

```yaml
mode: "server"
listen: "0.0.0.0:2020"
psk: "YOUR_SECRET_PSK"
profile: "stable"

# تانل معکوس (Reverse Tunnel)
forward:
  tcp:
    - "0.0.0.0:8080 -> 127.0.0.1:8080" # پورت 8080 ایران به 8080 خارج

smux:
  version: 2
  keepalive: 15
  frame_size: 2048    # بهینه‌سازی شده برای DPI
  max_recv: 2097152   # بافرهای بزرگ (2MB)
```

---

## 🛠️ دستورات پرکاربرد

### مدیریت سرویس
```bash
# مشاهده وضعیت و لاگ‌ها
journalctl -u picotun-server -f   # سرور
journalctl -u picotun-client -f   # کلاینت

# ریستارت
systemctl restart picotun-server
systemctl restart picotun-client
```

### بیلد دستی (بازنویسی)
نیاز به Go 1.21+:
```bash
git clone https://github.com/ramin-mahmoodi/tunnelR.git
cd tunnelR
go mod tidy
go build -o picotun cmd/picotun/main.go
```

---

<p align="center">
  Development by <strong>Ramin Mahmoodi</strong> | v2.5.0
</p>
